{% extends "pages/base.twig" %}

{% block content %}
	{% for post in posts %}
		{% include ['pieces/tease-'~post.post_type~'.twig', 'pieces/tease.twig'] %}
	{% endfor %}

	{% include 'pieces/pagination.twig' with { pagination: posts.pagination({show_all: false, mid_size: 3, end_size: 2}) } %}
{% endblock %}
